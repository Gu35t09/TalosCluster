kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: linkding-data-pvc
spec:
  storageClassName: local-storage
  resources:
    requests:
      storage: 1Gi
  accessModes:
    - ReadWriteOnce
